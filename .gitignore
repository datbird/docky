# Dependencies
node_modules
.pnpm-store

# Python
__pycache__/
*.pyc

# OS / editor
.DS_Store
*.swp
.vscode/settings.json

# Build output — rebuilt from src/ with `pnpm run build`. Not committed: the
# Decky store rebuilds the frontend from source (matching the official plugin
# template), so a committed bundle is redundant. install.sh requires you to
# build first; ship the bundle via Release zips for Node-less manual installs.
dist/
